{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local relay controls only; no frontend filesystem or shell access",
  "windows": ["main"],
  "permissions": ["core:default"]
}
